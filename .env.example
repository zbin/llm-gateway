PORT=3000
NODE_ENV=development
LOG_LEVEL=info
JWT_SECRET=your-secret-key-change-this-in-production
API_REQUEST_LOG_RETENTION_DAYS=3

# MySQL 数据库配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your-mysql-password
MYSQL_DATABASE=llm_gateway

# LLM Gateway 公网访问地址
# 如果部署在公网服务器上，请设置为实际的公网地址
# 例如: http://your-domain.com 或 http://your-ip:3000
# 此配置也可以在 Web UI 的系统设置页面中动态修改
PUBLIC_URL=http://localhost:3000

# 演示模式 (可选，默认关闭)
# 启用后将每隔 3 天自动清空数据库和 Portkey 配置，并创建演示用户 (demo/demo1234)
# 仅用于演示环境，生产环境请勿启用
# DEMO_MODE=true

# 消息压缩配置 (可选)
# 定义在压缩历史消息时要保留的最新消息数量
# 默认值为 2，表示保留最后两条消息不进行压缩
# 如果设置为 5，则保留最后五条消息
KEEP_RECENT_MESSAGES=5

