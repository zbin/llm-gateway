PORT=3000
NODE_ENV=development
DB_PATH=./data/gateway.db
PORTKEY_CONFIG_PATH=./portkey-config/conf.json
LOG_LEVEL=info
JWT_SECRET=your-secret-key-change-this-in-production
API_REQUEST_LOG_RETENTION_DAYS=3

# Portkey Gateway URL (可选，仅用于创建默认网关)
# 现在支持通过 Web UI 管理多个 Portkey Gateway 实例
PORTKEY_GATEWAY_URL=http://localhost:8787

# LLM Gateway 公网访问地址 (用于 Agent 回调)
# 如果部署在公网服务器上，请设置为实际的公网地址
# 例如: http://your-domain.com 或 http://your-ip:3000
PUBLIC_URL=http://localhost:3000

