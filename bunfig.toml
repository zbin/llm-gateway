# Bun 配置文件

# 包管理器配置
[install]
registry = "http://mirrors.tencent.com/npm/"
# 启用 workspace
auto = "auto"

# 运行时配置
[run]
# 启用 bun shell
shell = "bun"

# 测试配置
[test]
# 测试超时时间（毫秒）
timeout = 5000
# 预加载文件
# preload = []
